[Unit]
Description=TestApp
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www
ExecStart=/var/www/mvnw spring-boot:run -Dspring.config.location=/tmp/application.properties
Restart=on-failure

[Install]
WantedBy=multi-user.target
